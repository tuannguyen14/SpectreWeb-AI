# SpectreWeb AI v7.0.0 - Requirements
#
# INSTALLATION:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#   python3 server.py

# ============================================================================
# CORE FRAMEWORK
# ============================================================================
flask>=2.3.0             # Web framework for API server
requests>=2.31.0         # HTTP client
psutil>=5.9.0            # System/process utilities
fastmcp>=0.2.0           # MCP framework (FastMCP server)

# ============================================================================
# WEB SCRAPING & PARSING
# ============================================================================
beautifulsoup4>=4.12.0   # HTML parsing (bs4)

# ============================================================================
# MACHINE LEARNING (Self-Learning AI)
# ============================================================================
numpy>=1.24.0            # Numerical computing
scikit-learn>=1.3.0      # ML models for secret classification & endpoint scoring

# ============================================================================
# OPTIONAL DEPENDENCIES (uncomment if needed)
# ============================================================================
# aiohttp>=3.8.0         # Async HTTP (only if using async features)
# selenium>=4.15.0       # Browser automation (only if using browser agent)
# webdriver-manager>=4.0.0  # ChromeDriver management (only with selenium)
# mitmproxy>=9.0.0       # HTTP proxy interception (only for proxy features)
# pwntools>=4.10.0       # Binary exploitation (only for CTF/binary analysis)
# angr>=9.2.0            # Binary analysis (only for reverse engineering)

# ============================================================================
# EXTERNAL SECURITY TOOLS (Install separately via OS package manager)
# ============================================================================
# SpectreWeb integrates with external CLI tools that must be installed
# separately. Kali Linux includes most by default.
#
# Required for core features:
#   nmap, ffuf, subfinder, httpx, katana, whatweb, sqlmap, dalfox,
#   naabu, gau, waybackurls, arjun
#
# See README.md for full installation guide.
